SELECT OFFICER_RISK_RATING_DESC, COUNT(*) as count 
                     FROM CUSTOMER_DIMENSION 
                     WHERE OFFICER_RISK_RATING_DESC IS NOT NULL 
                     GROUP BY OFFICER_RISK_RATING_DESC 
                     ORDER BY count DESC